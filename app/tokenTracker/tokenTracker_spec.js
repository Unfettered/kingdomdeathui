'use strict';

describe('kingdom-death-ui.tokenTracker module', function() {

  beforeEach(module('kingdom-death-ui.tokenTracker'));

  describe('token tracker controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var tokenTrackerController = $controller('tokenTrackerController');
      expect(tokenTrackerController).toBeDefined();
    }));

  });
});