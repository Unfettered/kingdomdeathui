'use strict';

describe('kingdom-death-ui.setup module', function() {

  beforeEach(module('kingdom-death-ui.setup'));

  describe('setup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var setupController = $controller('setupController');
      expect(setupController).toBeDefined();
    }));

  });
});