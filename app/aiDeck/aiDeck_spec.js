'use strict';

describe('kingdom-death-ui.aiDeck module', function() {

  beforeEach(module('kingdom-death-ui.aiDeck'));

  describe('ai deck controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var aiDeckController = $controller('aiDeckController');
      expect(aiDeckController).toBeDefined();
    }));

  });
});