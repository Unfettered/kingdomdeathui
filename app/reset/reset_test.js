'use strict';

describe('kingdom-death-ui.reset module', function() {

  beforeEach(module('kingdom-death-ui.reset'));

  describe('reset controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var resetController = $controller('resetController');
      expect(resetController).toBeDefined();
    }));

  });
});