'use strict';

describe('kingdom-death-ui.hunt module', function() {

  beforeEach(module('kingdom-death-ui.hunt'));

  describe('hunt controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var huntController = $controller('huntController');
      expect(huntController).toBeDefined();
    }));

  });
});