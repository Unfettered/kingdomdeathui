'use strict';

describe('kingdom-death-ui.showdown module', function() {

  beforeEach(module('kingdom-death-ui.showdown'));

  describe('showdown controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var showdownController = $controller('showdownController');
      expect(showdownController).toBeDefined();
    }));

  });
});